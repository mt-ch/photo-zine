// PROTOTYPE — throwaway. Answers "what does a published zine look like?"
// (wayfinder ticket: Zine layout). Converged on a drag-to-explore infinite
// canvas; iterating on spacing/aspect-ratio/motion feel from here.

import { VariantD } from "./variant-d-grid";

export default function ZinePrototypePage() {
  return <VariantD />;
}
